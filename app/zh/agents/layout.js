// /zh/agents/* — same full-screen layout as /agents/*; the pages read the
// locale from the URL (middleware x-locale header), so copy and links stay Chinese.
export { default, generateMetadata } from '../../agents/layout';
