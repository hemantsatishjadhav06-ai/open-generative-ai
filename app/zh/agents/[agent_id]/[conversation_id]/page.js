// Additive locale route: the same page as /agents/[agent_id]/[conversation_id].
export { default, generateMetadata } from '../../../../agents/[agent_id]/[conversation_id]/page';

export const dynamic = 'force-dynamic';
